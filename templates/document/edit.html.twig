{% extends 'base.html.twig' %}

{% block title %}Edit Document{% endblock %}

{% block body %}
    <div class="container">
    <h1>Edit Document</h1>

    {{ include('document/_form.html.twig', {'button_label': 'Editer'}) }}

    <a class="btn btn-primary float-end" href="{{ path('document_index') }}">Retour a la liste</a>

    {{ include('document/_delete_form.html.twig') }}
    </div>
{% endblock %}

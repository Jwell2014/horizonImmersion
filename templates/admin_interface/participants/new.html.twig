{% extends 'base_admin.html.twig' %}

{% block title %}Nouveau Participant{% endblock %}

{% block body %}

    <div class="container newE1 bgNewE1">
        <div class="card mt-5 col-6 offset-3 bg-light">
            <h2 class="text-center my-5">Ajouter un participant</h2>
            {{ include('admin_interface/participants/_form.html.twig', {button_label: 'Créer'}) }}
            <div class="text-center">
                <a href="{{ path('app_participants_index') }}" class="btn buttonStyle3 mb-5">Retour</a>
            </div>
        </div>
    </div>
{% endblock %}

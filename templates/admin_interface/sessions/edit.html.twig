{% extends 'base_admin.html.twig' %}

{% block title %}Éditer une session{% endblock %}

{% block body %}
    <div class="container newE1 bgNewE1">
        <div class="card mt-5 col-6 offset-3 bg-light">
            <h2 class="text-center my-5">Éditer la session </h2>
            {{ include('admin_interface/sessions/_form.html.twig', {button_label: 'Éditer'}) }}
            <div class="text-center">
                <a href="{{ path('app_sessions_index') }}" class="btn buttonStyle2 mb-5">Retour</a>
            </div>
        </div>
    </div>
{% endblock %}

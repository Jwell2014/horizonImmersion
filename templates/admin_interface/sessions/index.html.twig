{% extends 'base_admin.html.twig' %}

{% block title %}Sessions{% endblock %}

{% block body %}
    <div class="row m-0">
        {{ parent() }}
        <div class="col-10 p-0">
            <div class="container">
                <div class="text-center">
                    <a href="{{ path('app_sessions_new') }}" class="btn buttonStyle2 mb-3 addRiddle1">Ajouter une
                        session</a>
                </div>
                <table class="table enigme1">
                    <thead class="tableHeadEnigme1 text-center">
                    <tr class="">
                        <th scope="col">Nom de la session</th>
                        <th scope="col">Nombre de participants</th>
                        <th scope="col">État de la session</th>
                        <th scope="col" class="ps-4">Action</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for session in sessions %}
                        <tr class="text-center">
                            <th class="table-light" scope="row">{{ session.nomDeSession }}</th>
                            <td class="table-light">{{ session.nombreParticipants }}</td>
                            <td class="table-light">{{ session.etatDeSession }}</td>
                            <td class="table-light">
                                <div class="row d-flex justify-content-center">
                                    <a href="{{ path('app_sessions_show', {'id': session.id}) }}"
                                       class="col-1 d-flex align-items-center"><i class="fa-solid fa-eye text-black me-5"></i></a>                                    <a href="{{ path('app_sessions_edit', {'id': session.id}) }}"
                                       class="col-1 d-flex align-items-center"><i
                                                class="fas fa-edit text-black ms-2"></i></a>
                                    <div class="col-1 ms-0">
                                    {{ include('admin_interface/sessions/_delete_form.html.twig') }}
                                    </div>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}

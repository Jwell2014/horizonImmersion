
<div class="container">

    {{ form_start(form) }}

    {{ form_row(form.nom) }}
    {{ form_row(form.image) }}

    {{ form_end(form) }}

    <button class="btn">{{ button_label|default('Save') }}</button>
</div>
